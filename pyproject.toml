[project]
name = "comfyui-fizgig-h3-still"
description = "True single-frame stills from MiniMax H3: a one-frame latent and a decode that renders it cleanly, at any size. Two nodes, no extra models."
version = "1.0.0"
license = { text = "MIT" }
dependencies = []

[project.urls]
Repository = "https://github.com/shootthesound/ComfyUI-Fizgig-H3-Still"

[tool.comfy]
PublisherId = "shootthesound"
DisplayName = "Fizgig H3 Still"
